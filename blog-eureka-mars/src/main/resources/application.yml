server:
  port: 37000
spring:
  application:
    name: blog-eureka-mars

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:37000/eureka
